import React from 'react'

import { Alert } from '@mui/material'

const NotFoundPage = () => {
  return (
    <div>
      <Alert severity="info">Diese Seite existiert nicht.</Alert>
    </div>
  )
}

export default NotFoundPage
